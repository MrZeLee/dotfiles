## Clipboard

When you need to copy something to my clipboard, always use `wl-copy`:

```bash
echo "text" | wl-copy
```

Never just print the text and ask me to copy it manually — use wl-copy directly.
